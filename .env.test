# Test environment variables for Playwright tests
# These are placeholder values that allow the app to run without real services

# Supabase placeholders - allows app to start without authentication
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key-for-testing

# Weather API - you can use a test key here if you have one
NEXT_PUBLIC_OPENWEATHER_API_KEY=test-api-key

# Other optional services (placeholders)
GOOGLE_POLLEN_API_KEY=test-pollen-key
GOOGLE_AIR_QUALITY_API_KEY=test-air-quality-key

# Disable telemetry for tests
NEXT_TELEMETRY_DISABLED=1

# Node environment
NODE_ENV=test
