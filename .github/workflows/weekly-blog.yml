name: Weekly Blog Post

on:
  schedule:
    - cron: '0 12 * * 0'  # Every Sunday at noon UTC
  workflow_dispatch: {}     # Manual trigger

permissions:
  contents: write

jobs:
  generate-blog-post:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate blog post
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: npx tsx scripts/generate-blog-post.ts

      - name: Check for new posts
        id: check
        run: |
          if git diff --name-only | grep -q 'content/blog/'; then
            echo "new_post=true" >> "$GITHUB_OUTPUT"
          else
            echo "new_post=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push new blog post
        if: steps.check.outputs.new_post == 'true'
        run: |
          git config user.name "16bitbot"
          git config user.email "16bitbot@16bitweather.co"
          git add content/blog/
          TITLE=$(grep -m1 '^title:' content/blog/$(git diff --name-only content/blog/ | head -1) | sed 's/title: "//;s/"$//')
          git commit -m "blog: ${TITLE:-new weekly dispatch}

          Auto-generated by 16bitbot weekly blog pipeline.

          Co-Authored-By: 16bitbot <16bitbot@16bitweather.co>"
          git push
